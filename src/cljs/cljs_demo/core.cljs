(ns ^:figwheel-no-load cljs-demo.core
  (:require [cljs-demo.movies :as movies]
            [cljs-demo.app :as app]
            [cljs-demo.comm :as comm]))

(enable-console-print!)

(defn fig-reload []
  ;(app/stop)
  ;(app/main)
  )

;(app/main)

